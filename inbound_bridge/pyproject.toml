[project]
name = "inbound-bridge"
version = "0.1.0"
description = "Inbound email webhook bridge for openkoutsi"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
    "sqlalchemy[asyncio]>=2.0",
    "aiosqlite",
    "pydantic-settings",
]
